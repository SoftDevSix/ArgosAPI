spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
server:
  port: 8080

logging:
  level:
    org.springframework: DEBUG

file:
  manager:
    base-url: http://localhost:8081/fileManager